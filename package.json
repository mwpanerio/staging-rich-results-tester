{
  "name": "staging-rich-results-tester",
  "version": "1.0.0",
  "description": "Local Rich Results tester for password-protected staging sites — crawl mobile/desktop, validate JSON-LD, and review HTML/screenshots without Googlebot access.",
  "type": "module",
  "scripts": {
    "start": "bash scripts/start.sh",
    "dev": "bash scripts/start.sh",
    "smoke": "node scripts/smoke.mjs",
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:mwpanerio/staging-rich-results-tester.git"
  },
  "dependencies": {
    "cheerio": "^1.1.0",
    "express": "^5.1.0",
    "puppeteer": "^24.15.0"
  }
}
